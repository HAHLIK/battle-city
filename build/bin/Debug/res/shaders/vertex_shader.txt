#version 460 core

layout(location = 0) in vec3 vertex_point;
layout(location = 1) in vec2 texture_point;
out vec2 texCoord;

void main()
{
   gl_Position = vec4(vertex_point, 1.0);
   texCoord = texture_point;
}